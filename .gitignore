node_modules
.next
next-env.d.ts
site/
.venv-docs/

# Secrets — never commit
.env
.env.local
.env*.local

# Personal config and scored snapshots
assessment.config.json
app/data/assessment.json
app/data/assessment-history.json
app/data/progression.json
app/data/progression-config.json
app/data/insights-narrative.md

# Snapshot of the /boris skill — third-party content (Boris Cherny / Daniel An).
# Regenerated locally by `npm run snapshot:boris-tips` (also runs as a
# postinstall hook). Not redistributed via this MIT-licensed repo.
app/data/boris-tips-content.json

# Installed LaunchAgent's runtime logs
.launchd.out.log
.launchd.err.log

# Tooling artefacts
.DS_Store
.playwright-mcp
dashboard-*.png
.tmp

# graphify knowledge-graph build output (regenerated by `/graphify`).
# ~3 MB per build (graph.json + graph.html); the extraction cache under
# graphify-out/cache/ is machine-local and keyed to absolute paths.
graphify-out/

# Test artefacts
coverage/
playwright-report/
test-results/
.launchd-coverage.out.log
.launchd-coverage.err.log

# Coverage data files (regenerated daily by the routine)
app/data/coverage.json
app/data/coverage-history.json

# User-specific Claude Code settings (the command file in .claude/commands stays committed)
.claude/settings.local.json
.claude/*.bak
.claude/scheduled_tasks.lock
.claude/worktrees/

# Python bytecode — docs/superpowers/retrospectives/artifacts/ carries .py
# primary sources that get compiled when someone syntax-checks them.
__pycache__/

# TypeScript incremental build artifact
tsconfig.tsbuildinfo
